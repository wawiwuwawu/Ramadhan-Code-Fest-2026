@model List<client.Models.KategoriModel>
@{
    ViewBag.Title = "Daftar Kategori";
}

<h2>Daftar Kategori Buku</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <i class="glyphicon glyphicon-ok-sign"></i> @TempData["SuccessMessage"]
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <i class="glyphicon glyphicon-exclamation-sign"></i> @TempData["ErrorMessage"]
    </div>
}

<div class="row" style="margin-bottom: 15px;">
    <div class="col-md-6">
        <a href="@Url.Action("Create")" class="btn btn-primary">
            <i class="glyphicon glyphicon-plus"></i> Tambah Kategori Baru
        </a>
        <a href="@Url.Action("Index", "Home")" class="btn btn-default">
            <i class="glyphicon glyphicon-arrow-left"></i> Kembali ke Dashboard
        </a>
    </div>
    <div class="col-md-6">
        @using (Html.BeginForm("Index", "Kategori", FormMethod.Get, new { @class = "form-inline pull-right" }))
        {
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="Cari nama kategori..." value="@ViewBag.SearchKeyword" style="width: 250px;">
                <span class="input-group-btn">
                    <button class="btn btn-default" type="submit">
                        <i class="glyphicon glyphicon-search"></i> Cari
                    </button>
                </span>
            </div>
            
            if (!string.IsNullOrEmpty(ViewBag.SearchKeyword as string))
            {
                <a href="@Url.Action("Index")" class="btn btn-link">
                    <i class="glyphicon glyphicon-remove"></i> Reset
                </a>
            }
        }
    </div>
</div>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Nama Kategori</th>
            <th>Aksi</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Count > 0)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.id_kategori</td>
                    <td>@item.nama_kategori</td>
                    <td>
                        <a href="@Url.Action("Edit", new { id = item.id_kategori })" class="btn btn-warning btn-sm">
                            <i class="glyphicon glyphicon-pencil"></i> Edit
                        </a>
                        <a href="@Url.Action("Delete", new { id = item.id_kategori })" class="btn btn-danger btn-sm" onclick="return confirm('Yakin ingin menghapus kategori ini?')">
                            <i class="glyphicon glyphicon-trash"></i> Hapus
                        </a>
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="3" class="text-center">
                    @{
                        if (!string.IsNullOrEmpty(ViewBag.SearchKeyword as string))
                        {
                            <text>Tidak ada kategori yang sesuai dengan pencarian "<strong>@ViewBag.SearchKeyword</strong>"</text>
                        }
                        else
                        {
                            <text>Tidak ada data kategori</text>
                        }
                    }
                </td>
            </tr>
        }
    </tbody>
</table>
