@model List<client.Models.BukuModel>
@{
    ViewBag.Title = "Daftar Buku";
}

<h2>Daftar Buku Perpustakaan</h2>

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success alert-dismissible">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <i class="glyphicon glyphicon-ok-sign"></i> @TempData["SuccessMessage"]
    </div>
}

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger alert-dismissible">
        <button type="button" class="close" data-dismiss="alert">&times;</button>
        <i class="glyphicon glyphicon-exclamation-sign"></i> @TempData["ErrorMessage"]
    </div>
}

<div class="row" style="margin-bottom: 15px;">
    <div class="col-md-6">
        <a href="@Url.Action("Create")" class="btn btn-primary">
            <i class="glyphicon glyphicon-plus"></i> Tambah Buku Baru
        </a>
        <a href="@Url.Action("Index", "Home")" class="btn btn-default">
            <i class="glyphicon glyphicon-arrow-left"></i> Kembali ke Dashboard
        </a>
    </div>
    <div class="col-md-6">
        @using (Html.BeginForm("Index", "Buku", FormMethod.Get, new { @class = "form-inline pull-right" }))
        {
            <div class="input-group">
                <input type="text" name="search" class="form-control" placeholder="Cari Kode, Judul, Penulis..." value="@ViewBag.SearchKeyword" style="width: 300px;">
                <span class="input-group-btn">
                    <button class="btn btn-default" type="submit">
                        <i class="glyphicon glyphicon-search"></i> Cari
                    </button>
                </span>
            </div>
            
            
            if (!string.IsNullOrEmpty(ViewBag.SearchKeyword as string))
            {
                <a href="@Url.Action("Index")" class="btn btn-link">
                    <i class="glyphicon glyphicon-remove"></i> Reset
                </a>
            }
        }
    </div>
</div>

<table class="table table-striped table-bordered">
    <thead>
        <tr>
            <th>ID</th>
            <th>Kode Buku</th>
            <th>Judul Buku</th>
            <th>Penulis</th>
            <th>Kategori</th>
            <th>Stok</th>
            <th>Aksi</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Count > 0)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.id_buku</td>
                    <td>@item.kode_buku</td>
                    <td>@item.judul_buku</td>
                    <td>@item.penulis</td>
                    <td>@(item.kategori != null ? item.kategori.nama_kategori : "-")</td>
                    <td>@item.stok</td>
                    <td>
                        <a href="@Url.Action("Edit", new { id = item.id_buku })" class="btn btn-warning btn-sm">
                            <i class="glyphicon glyphicon-edit"></i> Edit
                        </a>
                        @using (Html.BeginForm("DeleteConfirmed", "Buku", new { id = item.id_buku }, FormMethod.Post, new { style = "display:inline;" }))
                        {
                            <button type="submit" class="btn btn-danger btn-sm" onclick="return confirm('Yakin ingin menghapus buku ini?')">
                                <i class="glyphicon glyphicon-trash"></i> Hapus
                            </button>
                        }
                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="8" class="text-center">
                    @{
                        if (!string.IsNullOrEmpty(ViewBag.SearchKeyword as string))
                        {
                            <text>Tidak ada buku yang sesuai dengan pencarian "<strong>@ViewBag.SearchKeyword</strong>"</text>
                        }
                        else
                        {
                            <text>Tidak ada data buku</text>
                        }
                    }
                </td>
            </tr>
        }
    </tbody>
</table>

